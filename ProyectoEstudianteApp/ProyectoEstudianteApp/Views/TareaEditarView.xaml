<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ProyectoEstudianteApp.Views.TareaEditarView">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="EDITAR TAREA"
                   TextColor="Black"
                VerticalOptions="Start" 
                HorizontalOptions="CenterAndExpand" />
            <Label Text="Nombre Tarea"/>
            <Entry Text="{Binding Tarea.NombreTarea}"/>
            <Label Text="Due Date"/>
            <Entry Text="{Binding Tarea.DueDate}"/>
            <Button Text="Eliminar Tarea" 
                    Command="{Binding EliminarTarea}"/>
            <Button Text="Editar" 
                    Command="{Binding EditarTarea}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>